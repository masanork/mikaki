# Project status

This page distinguishes deployed behavior, local verification, and planned work. It reflects evidence recorded through 2026-09-24; later changes need their own verification.

| Capability | Evidence | Limit |
| --- | --- | --- |
| Portable WebAuthn verifier | Native and Wasm adapters passed all 155 required tests in the recorded FIDO2 Server Conformance Tools 1.9.1 run. | No formal FIDO certification or general security assurance. See the [test record](../local/conformance/results-2026-09-22.md). |
| Local login slice | Disposable local Workers and D1 exercise invitation enrollment, discoverable passkey login, OIDC Code + PKCE, RP session creation, and logout. | A test harness, not a production deployment. See [local development](getting-started.md). |
| Rust OIDC Worker | Local workerd probes cover selected code exchange, UserInfo, replay, concurrency, and client authentication. | A probe is not full interoperability or production readiness. See [OIDC conformance](oidc-core-conformance.md). |
| OIDC conformance | A local OIDF Config OP plan passed. A local Basic OP plan finished with 21 passed, 8 skipped, 4 manual review, 2 warning, and 0 failed modules. | Manual review and warnings remain; there is no formal certification or hosted conformance deployment. |
| Normal-profile Worker | The OP Worker and D1 are deployed for `https://mikaki.tossa.app`. One administrator account and credential exist in production. The administrator previously saved and reopened a Vault name. | Public TLS handshakes were reset from this network after the latest deployment, so current HTTP reachability is unverified. No registered production RP has been demonstrated. See [deployment](cloudflare-deployment.md). |
| Runtime policy | D1 contains an active versioned policy; selected Worker projections and a change CLI exist. | Not every example TOML field has a complete product loader or enforcement path. See [runtime configuration](runtime-configuration.md). |
| Vault | An owner-only R2 bucket, D1 migrations through `0008`, cleanup trigger, and page are deployed. Local Chromium exercises enrollment with PRF, encrypted name creation, reload/unlock, and update through the Worker. On 2026-09-24 the production administrator saved `name` with a real Passkey, reloaded the page, and decrypted the saved value; D1 reported active revision 1. A local workerd test exercises opt-in system sharing and revocation. | Cross-device restore, same-account replacement/recovery, and production conflict/failure cases remain unverified. Credentials without PRF cannot open the Vault. The deployed sharing policy remains disabled. |
| UserInfo recipient keys | Generation 1 is active in production D1 with a Secrets Store seed. The deployed claim Worker verified its public key and accepted a synthetic product envelope through the remote service binding, while rejecting owner/ciphertext substitutions. The browser validator gates candidate envelope creation. | A production owner has not approved a system Grant. The positive directory response has not been tested with a production owner session. RP claim release remains unavailable. See the [key lifecycle contract](vault-recipient-key-lifecycle.md). |
| Post-quantum cryptography | ML-KEM-768 Vault recipient sender and receiver candidate modules are in product source. Node, Chromium, and independent RustCrypto tests exercise their HPKE envelope and binding checks. A D1-disabled owner sharing path stores an envelope and Grant atomically; local workerd tests exercise it. | No post-quantum algorithm is enabled for product passkeys, OIDC signing, or production Vault sharing. UserInfo claim release is not implemented. See the [rollout plan](pqc-rollout.md). |
| Supply-chain attestations | CI builds and attests a Worker archive, Rust SBOM, and source-based CBOM. | The currently deployed Worker was uploaded from a developer machine; CI attestations do not identify its running bytes. See [supply-chain evidence](supply-chain.md). |

## Release blockers

Before use by real users, register and exercise production RPs; complete account and administrator enrollment; verify passkeys and PRF with intended devices; validate callback, session, and logout behavior end to end; exercise operational recovery and monitoring; and resolve the relevant conformance and security review gates. The [deployment guide](cloudflare-deployment.md) records the environment-specific state.

Vault system sharing exists behind a disabled policy; RP-specific claim release, file storage APIs, conversation archiving, federation, and MCP access remain proposals in the [roadmap](roadmap.md). A system Grant alone does not make UserInfo claim sharing available.
